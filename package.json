{
  "name": "media-verifier-using-playwright",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "rm -f results.ndjson.lock && npx playwright test",
    "verify": "node src/groupByExtension.js && rm -f results.ndjson.lock && npx playwright test",
    "report": "node generate-html-report.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0"
  }
}
